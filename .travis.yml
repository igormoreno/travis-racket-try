language: c

services:
  - docker

before_install:
  - docker pull igormoreno/usi-pf1

script:
  - docker container run -it --rm --mount type=bind,source="$(pwd)",target=/app --workdir=/app igormoreno/usi-pf1 ../utils/docker-run-all.sh
